using System;

namespace MonoBehaviours_Opdr4_Boyd
{
    public static class Program
    {
        [STAThread]
        static void Main()
        {
            using (var game = new Game1())
                game.Run();
        }
    }
}
